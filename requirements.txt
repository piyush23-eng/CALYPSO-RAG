# ── Core Agentic RAG & API Serving ──────────────────────────────────────
fastapi>=0.110.0
uvicorn>=0.28.0
pydantic>=2.7.0
langgraph>=0.2.0
httpx>=0.27.0
requests>=2.31.0

# ── Hybrid Retrieval & Vector Databases ────────────────────────────────
sentence-transformers>=3.0.0
chromadb>=0.5.0
rank-bm25>=0.2.2
qdrant-client>=1.9.0
numpy>=1.26.0
tqdm>=4.66.0

# ── Symbolic Mathematics & Dimensional Invariant Verification ──────────
sympy>=1.12.0
pint>=0.23.0

# ── Multimodal Neural Voice Synthesis ──────────────────────────────────
edge-tts>=6.1.0

# ── Empirical Evaluation & Testing Harness ─────────────────────────────
pytest>=8.0.0
datasets>=2.18.0
ragas>=0.1.0

# ── Model Fine-Tuning & Quantization (QLoRA) ───────────────────────────
torch>=2.2.0
transformers>=4.40.0
peft>=0.10.0
accelerate>=0.29.0
bitsandbytes>=0.43.0

# ── Optional: High-Throughput Continuous Batching GPU Serving ──────────
# NOTE: vllm is optional and requires Linux + CUDA (NVIDIA GPU).
# Install separately on CUDA hosts: pip install vllm>=0.4.0
